using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class BulletSpawner : MonoBehaviour
{
    // Start is called before the first frame update
    [Header("Spawner atributes")]
    public bool active;



    [Header("Spawnee atributes")]
    public GameObject bullet;
    public GameObject LBSObject=null;//last bullet spawned game object

    void Start()
    {
        active = true;
    }

    public void ShootBullet(Transform position)
    {
        if (LBSObject!=null)
        {
            Debug.Log("Spawner side is good");
            LBSObject.GetComponent<Bullet>().Shoot(position);
            
        }
        
    }
    public void DestroySpawner()
    {
        Destroy(gameObject);
    }
}
*/